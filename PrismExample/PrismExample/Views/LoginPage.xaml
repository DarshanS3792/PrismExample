<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PrismExample.Views.LoginPage"
             xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Padding="20">
            <Label Text="Welcome to Xamarin.Forms! Let us make use of PRISM and try few examples"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Entry Text="{Binding UserName}" 
                 Placeholder="User" 
                 HorizontalOptions="FillAndExpand"/>

            <Entry Placeholder="Password" 
                 IsPassword="true" 
                 HorizontalOptions="FillAndExpand">
                <Entry.Behaviors>
                    <b:EventToCommandBehavior EventName="Completed" 
                                          Command="{Binding OnLoginCommand}" />
                </Entry.Behaviors>
            </Entry>

            <Button Command="{Binding OnLoginCommand}" 
                  Text="Login" TextColor="White"
                  BackgroundColor="DarkSlateBlue" 
                  HorizontalOptions="FillAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>