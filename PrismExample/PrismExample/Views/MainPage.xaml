<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PrismExample.MainPage"
             BackgroundColor="White">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" Padding="20" Spacing="15">
            <!-- Place new controls here -->
            <Label Text="{Binding Title}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" FontSize="Small" />

            <Button Text="Display Alert" BackgroundColor="DarkSlateBlue" TextColor="White" Command="{Binding DisplayAlertCommand}"/>

            <Button Text="Go to Home Page" BackgroundColor="DarkSlateBlue" TextColor="White" Command="{Binding GotoHomePageCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
